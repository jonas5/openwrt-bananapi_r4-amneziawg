name: Build AmneziaWG OpenWrt Packages

on:
  workflow_dispatch:
    inputs:
      openwrt_version:
        description: "OpenWrt version (e.g., 25.12.2, leave empty for snapshot)"
        required: false
        default: ""
      openwrt_architecture:
        description: "Target architecture (e.g., mediatek/filogic, bcm2712)"
        required: false
        default: "mediatek/filogic"
  push:
    branches: [ main, master ]
    paths-ignore:
      - 'README.md'
      - 'configs/**'

permissions:
  contents: write

env:
  JOBS: 2

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: "mediatek/filogic"
            device: "bananapi_bpi-r4"
            arch_name: "mediatek-filogic"
          - arch: "bcm2712"
            device: ""
            arch_name: "bcm27xx-bcm2712"
          - arch: "armsr/armv8"
            device: ""
            arch_name: "armsr-armv8"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check if should build
        id: should-build
        run: |
          REQUESTED_ARCH="${{ github.event.inputs.openwrt_architecture }}"
          CURRENT_ARCH="${{ matrix.arch }}"
          
          # If dispatch with specific arch, only build matching
          if [ -n "$REQUESTED_ARCH" ] && [ "$REQUESTED_ARCH" != "$CURRENT_ARCH" ]; then
            echo "Skipping: requested $REQUESTED_ARCH, this job is $CURRENT_ARCH"
            echo "should_build=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          echo "should_build=true" >> $GITHUB_OUTPUT

      - name: Set up build environment
        if: steps.should-build.outputs.should_build == 'true'
        id: setup
        run: |
          # Determine version
          if [ -n "${{ github.event.inputs.openwrt_version }}" ]; then
            VERSION="${{ github.event.inputs.openwrt_version }}"
          else
            VERSION="snapshot"
          fi
          
          # Remove 'v' prefix if present
          VERSION="${VERSION#v}"
          
          # Determine architecture (use matrix for push, inputs for dispatch)
          if [ -n "${{ github.event.inputs.openwrt_architecture }}" ]; then
            ARCH="${{ github.event.inputs.openwrt_architecture }}"
          else
            ARCH="${{ matrix.arch }}"
          fi
          
          # Handle comma-separated architectures by using only the first one for dispatch
          if [[ "$ARCH" == *,* ]]; then
            ARCH=$(echo "$ARCH" | cut -d',' -f1)
          fi
          
          # Determine architecture name (use matrix value)
          ARCH_NAME="${{ matrix.arch_name }}"
          if [ -z "$ARCH_NAME" ]; then
            if [[ "$ARCH" == "bcm2712" ]]; then
              ARCH_NAME="bcm27xx-bcm2712"
            else
              ARCH_NAME=$(echo "$ARCH" | tr '/' '-')
            fi
          fi
          
          # Determine device
          DEVICE="${{ matrix.device }}"
          if [[ "$ARCH" == "mediatek/filogic" ]] && [ -z "$DEVICE" ]; then
            DEVICE="bananapi_bpi-r4"
          fi
          
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "ARCH=$ARCH" >> $GITHUB_ENV
          echo "ARCH_NAME=$ARCH_NAME" >> $GITHUB_ENV
          echo "DEVICE=$DEVICE" >> $GITHUB_ENV
          
          echo "Building: $ARCH (device: $DEVICE)"

      - name: Cache OpenWrt SDK
        if: steps.should-build.outputs.should_build == 'true'
        id: cache-sdk
        uses: actions/cache@v4
        with:
          path: openwrt-sdk-*/
          key: ${{ runner.os }}-sdk-${{ env.VERSION }}-${{ env.ARCH }}-${{ env.DEVICE || 'generic' }}
          restore-keys: |
            ${{ runner.os }}-sdk-${{ env.VERSION }}-${{ env.ARCH }}-

      - name: Download OpenWrt SDK
        if: steps.should-build.outputs.should_build == 'true' && steps.cache-sdk.outputs.cache-hit != 'true'
        run: |
          # Determine SDK path (bcm2712 uses bcm27xx subdirectory)
          if [[ "${{ env.ARCH }}" == "bcm2712" ]]; then
            SDK_TARGET_PATH="bcm27xx/bcm2712"
          else
            SDK_TARGET_PATH="${{ env.ARCH }}"
          fi
          
          # Determine download URL
          if [ "${{ env.VERSION }}" = "snapshot" ]; then
            SDK_BASE_URL="https://downloads.openwrt.org/snapshots/targets/${SDK_TARGET_PATH}/"
          else
            SDK_BASE_URL="https://downloads.openwrt.org/releases/${{ env.VERSION }}/targets/${SDK_TARGET_PATH}/"
          fi
          
          echo "Fetching SDK list from: $SDK_BASE_URL"
          
          # Get SDK filename (matches openwrt-sdk-<version>-<arch>_<compiler>.Linux-x86_64.tar.zst)
          SDK_FILE=$(curl -s "$SDK_BASE_URL" | grep -o "openwrt-sdk-[^\"]*\.tar\.\(xz\|zst\)" | head -n1)
          
          if [ -z "$SDK_FILE" ]; then
            echo "ERROR: SDK file not found for ${{ env.ARCH_NAME }}"
            curl -s "$SDK_BASE_URL" | grep -o 'openwrt-sdk[^"]*' | head -10
            exit 1
          fi
          
          echo "Downloading SDK: $SDK_FILE"
          wget --show-progress -O "openwrt-sdk.tar.xz" "${SDK_BASE_URL}${SDK_FILE}"
          
          echo "Extracting SDK..."
          tar -xf openwrt-sdk.tar.xz
          rm openwrt-sdk.tar.xz
          
          SDK_DIR=$(find . -maxdepth 1 -name "openwrt-sdk-*" -type d | head -n1)
          echo "SDK extracted to: $SDK_DIR"

      - name: Setup SDK and copy packages
        if: steps.should-build.outputs.should_build == 'true'
        run: |
          SDK_DIR=$(find . -maxdepth 1 -name "openwrt-sdk-*" -type d | head -n1)
          WORKSPACE_DIR="${{ github.workspace }}"
          
          cd "$SDK_DIR"
          
          echo "Setting up SDK..."
          
          # Update feeds
          echo "Updating feeds..."
          ./scripts/feeds update -a || true
          ./scripts/feeds install -a || true
          
          # Copy local packages
          echo "Copying packages..."
          for pkg in "$WORKSPACE_DIR"/package/*; do
            if [ -d "$pkg" ]; then
              PKG_NAME=$(basename "$pkg")
              echo "  Copying $PKG_NAME"
              rm -rf "package/$PKG_NAME"
              cp -r "$pkg" "package/"
            fi
          done

      - name: Configure packages
        if: steps.should-build.outputs.should_build == 'true'
        run: |
          SDK_DIR=$(find . -maxdepth 1 -name "openwrt-sdk-*" -type d | head -n1)
          cd "$SDK_DIR"
          
          echo "Configuring packages..."
          
          # Base package config
          echo "CONFIG_PACKAGE_kmod-amneziawg=m" > .config
          echo "CONFIG_PACKAGE_amneziawg-tools=y" >> .config
          echo "CONFIG_PACKAGE_luci-proto-amneziawg=y" >> .config
          echo "CONFIG_PACKAGE_kmod-crypto-lib-chacha20=m" >> .config
          echo "CONFIG_PACKAGE_kmod-crypto-lib-chacha20poly1305=m" >> .config
          echo "CONFIG_PACKAGE_kmod-crypto-lib-curve25519=m" >> .config
          echo "CONFIG_PACKAGE_ethtool=m" >> .config
          
          # Device-specific config
          if [ "${{ env.DEVICE }}" = "bananapi_bpi-r4" ]; then
            echo "Adding BPI-R4 device config..."
            echo "CONFIG_TARGET_DEVICE_mediatek_filogic_DEVICE_bananapi_bpi-r4=y" >> .config
            echo "CONFIG_TARGET_mediatek_filogic_DEVICE_bananapi_bpi-r4=y" >> .config
            echo "CONFIG_TARGET_BOARD=mediatek" >> .config
            echo "CONFIG_TARGET_PROFILE=DEVICE_bananapi_bpi-r4" >> .config
            echo "CONFIG_HAS_SUBTARGETS=y" >> .config
            echo "CONFIG_TARGET_SUBTARGET=filogic" >> .config
            echo "CONFIG_NET_UDP_TUNNEL=m" >> .config
            echo "CONFIG_CRYPTO_CURVE25519=y" >> .config
          fi
          
          # Regenerate config
          make defconfig
          
          echo "Configuration complete"

      - name: Build AmneziaWG packages
        if: steps.should-build.outputs.should_build == 'true'
        run: |
          SDK_DIR=$(find . -maxdepth 1 -name "openwrt-sdk-*" -type d | head -n1)
          cd "$SDK_DIR"
          
          echo "Building packages for ${{ env.ARCH }}..."
          
          # Build packages in order
          for PKG in luci-proto-amneziawg kmod-amneziawg amneziawg-tools; do
            echo ""
            echo "=========================================="
            echo "Building: $PKG"
            echo "=========================================="
            
            # Clean, download, prepare
            make package/$PKG/{clean,download,prepare} V=s || true
            
            # Compile
            if ! make package/$PKG/compile -j${{ env.JOBS }} V=s; then
              echo "ERROR: Failed to build $PKG"
              exit 1
            fi
          done
          
          echo ""
          echo "=========================================="
          echo "Build completed successfully!"
          echo "=========================================="
          
          # List built packages
          echo "Built packages:"
          find bin/ -name "*.apk" -o -name "*.ipk" | grep -E "(amneziawg|kmod)" | head -20

      - name: Collect artifacts
        if: steps.should-build.outputs.should_build == 'true'
        run: |
          SDK_DIR=$(find . -maxdepth 1 -name "openwrt-sdk-*" -type d | head -n1)
          cd "${{ github.workspace }}"
          
          # Copy packages to architecture-specific directory
          mkdir -p artifacts/${{ env.ARCH_NAME }}
          
          find "$SDK_DIR/bin" -name "amneziawg-tools*.apk" -exec cp {} artifacts/${{ env.ARCH_NAME }}/ \; 2>/dev/null || true
          find "$SDK_DIR/bin" -name "luci-proto-amneziawg*.apk" -exec cp {} artifacts/${{ env.ARCH_NAME }}/ \; 2>/dev/null || true
          find "$SDK_DIR/bin" -name "kmod-amneziawg*.apk" -exec cp {} artifacts/${{ env.ARCH_NAME }}/ \; 2>/dev/null || true
          
          # Create archive with architecture in filename
          cd artifacts
          if [ "$(ls -A)" ]; then
            tar -cJf amneziawg-${{ env.ARCH_NAME }}-${{ env.VERSION }}.tar.xz ${{ env.ARCH_NAME }}/*
          fi
          
          echo "Artifacts:"
          ls -lah
          ls -lah ${{ env.ARCH_NAME }}/

      - name: Create GitHub Release
        if: steps.should-build.outputs.should_build == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: build-${{ env.VERSION }}-${{ env.ARCH_NAME }}-${{ env.DEVICE || 'generic' }}
          name: "AmneziaWG OpenWrt ${{ env.VERSION }} (${{ env.ARCH_NAME }})"
          body: |
            ## AmneziaWG OpenWrt Packages
            
            **OpenWrt Version:** ${{ env.VERSION }}
            **Architecture:** ${{ env.ARCH }}
            **Device:** ${{ env.DEVICE || 'generic' }}
            **Build Date:** ${{ github.event.head_commit.timestamp }}
            
            ### Packages Included:
            - **amneziawg-tools** - Userspace control program
            - **kmod-amneziawg** - Linux kernel module  
            - **luci-proto-amneziawg** - LuCI web interface protocol
            
            ### Installation:
            ```bash
            apk add amneziawg-tools_*.apk
            apk add luci-proto-amneziawg_*.apk
            apk add kmod-amneziawg_*.apk
            ```
            
            ### Source:
            - Repository: ${{ github.repository }}
            - Commit: ${{ github.sha }}
          draft: false
          prerelease: ${{ env.VERSION == 'snapshot' }}
          files: artifacts/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts
        if: steps.should-build.outputs.should_build == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: amneziawg-${{ env.ARCH_NAME }}-${{ env.VERSION }}-${{ strategy.job-index }}
          path: artifacts/**
          overwrite: true
